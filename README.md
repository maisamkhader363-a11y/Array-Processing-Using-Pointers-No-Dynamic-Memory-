# Array-Processing-Using-Pointers-No-Dynamic-Memory-
Developed a C++ program for array processing using strict pointer arithmetic without relying on array indexing (e.g., arr[i] is prohibited) or dynamic memory allocation. The project demonstrates a solid understanding of low-level memory traversal by performing core operations on a static integer array using pointers only.
